# Tools

Reusable developer, schema, build, generation, inspection, migration, and analysis tools live here.

Tools are owned components with documentation and tests. Repository [`../scripts/`](../scripts/) should remain thin entry points that invoke reusable tool logic.

A tool must not quietly become a second implementation of production behavior.
