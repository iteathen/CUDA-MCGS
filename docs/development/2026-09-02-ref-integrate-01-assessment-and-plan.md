# REF-INTEGRATE-01 assessment and execution plan

**Date:** 2026-09-02

**Parent:** #36 `ENGINE-REFERENCE-01`

**Exact input:** `experimental/portfolio@85c20d794746031d201f72bc78fce25ff9f69c3d`, tree `5eadec7f403a388ea096f983ebf2e87eca2465f0`

**Protected authority:** `main@3ecac11e3576bd063760bc9572f79bea78acd031` — protected unchanged input, not a write target.

**Focus branch:** `REF-INTEGRATE-01` / Git branch `ref/integrate-01`

## Assessment

All dependency leaves required by the governing ENGINE-REFERENCE plan are now candidate-integrated through the exact input above. The integrated packet contains the neutral/Domain harness, the complete Graph chain, Policy, Evaluator, Resource, Progress, Output, Framework lifecycle, terminal Session-absent integration, optional Session, optional Stage, and the Channel owner-evidence reuse leaf.

The remaining demonstrated gap is integration evidence, not another semantic interpreter. `experiments/search-semantics-reference/run.mjs` still explicitly limits itself to the neutral harness plus Domain and disclaims later owners. Later owner runners write separate canonical evidence files, but no final owner-neutral packet currently proves that those exact leaves agree on one Composer identity, that all planned reference routes are represented exactly once by their natural owners, that optional deletion/multi-instance/schedule evidence is present, that native-deferred routes remain deferred, or that the resulting evidence/cleanup identities form one frozen handoff for #122.

### Strongest overengineering challenge

A monolithic CPU reference engine, general model checker, workflow DSL, second Channel state machine, or production-shaped composition runtime would duplicate semantic authority and violate LEGO ownership. Reject all of them. The final leaf needs only a strict verifier over already generated owner evidence plus a small integration-only evidence manifest/case bank.

### Strongest underengineering challenge

A prose-only index of prior run IDs would not falsify missing owners, stale evidence identity, route-count drift, deletion/multi-instance gaps, native-disposition promotion, product leakage, or disagreement between the optional leaves and the core packet. Reject prose-only reconciliation.

### Disposition

**Proceed** with a thin final evidence verifier. It may read owner evidence artifacts, checked-in coverage classifications, and checked-in integration fixture expectations. It must not import owner semantic modules, normalize owner state, invent schedules for owner behavior, or reinterpret native requirements.

## Ownership and boundaries

`REF-INTEGRATE-01` owns only cross-owner evidence reconciliation and the final CUDA-free reference packet identity.

It does not own:

- Domain, Graph, Policy, Evaluator, Resource, Progress, Output, Session, Stage, Channel, or Framework semantic state;
- resource accounting or global progress decisions;
- CUDA release/acquire, native scheduling, physical races, occupancy, performance, or exact compatible-pair qualification;
- chess, UCI, model, Book Forge, Timing Evidence, tablebase, or other product meaning;
- protected #122 acceptance.

## Inputs

The verifier consumes only public/generated evidence outputs from existing runners:

- Composer evidence and profile projections;
- neutral/Domain evidence;
- Graph NODE, EDGE, REF, PATH, ROOT, RECLAIM, ADVANCE occurrence, and CLEANUP evidence;
- Policy, Evaluator, Resource, Progress, Output evidence;
- Framework lifecycle evidence;
- terminal-slice evidence;
- Session evidence;
- Stage evidence;
- Channel owner-evidence manifest/evidence;
- `schemas/search-ir/0.2.0/requirement-coverage.json` as the classification source.

Generated `build/` output remains ignored and disposable.

## Required integration checks

1. Every required evidence input exists, has an expected capsule/schema, reports pass, exact discovery/execution where applicable, zero failures, zero skip classes, and zero not-discovered residue.
2. Every evidence input that consumes Composer composition identity agrees on the exact same canonical Composer key.
3. The final packet accounts for all planned `ENGINE-REFERENCE-01` direct behavioral routes plus the 41 Channel routes through owner evidence; it must not silently count native-compatible-pair requirements as reference-complete.
4. Channel remains evidence reuse: the final verifier consumes `channel-evidence.json` and never imports/reconstructs Channel semantics.
5. The packet proves materially different product-neutral instances and legal schedule histories by requiring existing owner cases/identities that already exercise those distinctions.
6. Optional deletion/absence is explicit. Required evidence must include evaluator/live-output/Session/Stage/Channel/capability/product deletion or zero-residue cases already owned by their respective capsules/Composer evidence.
7. Multi-device treatment is limited to finite independent semantic replicas of the already frozen packet. Integration may prove replica independence and device-label non-authority; it may not claim multi-GPU support or define cross-device coordination semantics.
8. Native-compatible-pair routes remain classified/deferred. The integration leaf fails if any are promoted to reference evidence.
9. The final evidence identity is content-sensitive to the exact owner evidence identities, coverage classification source, integration fixture, and verifier source.
10. No product-specific vocabulary or native implementation path enters the final packet.

## Cheapest decisive falsifiers

Permanent integration mutations must detect at least:

- one missing owner evidence packet;
- one substituted/stale evidence identity;
- one missing required deletion/multi-instance/schedule witness;
- one native-deferred route falsely promoted into reference completion;
- one replica whose semantic packet diverges from the frozen base or whose device label changes semantic identity;
- one Channel route/owner-case mismatch;
- one final evidence identity mutation.

A pre-green run on the first integration verifier revision should fail on the real current gap: the final checked-in integration fixture/evidence contract is absent. The repair adds only the final reconciliation fixture/cases, not owner semantics.

## Execution sequence

1. Add the thin integration verifier, fixture schema/expected witness manifest, script entrypoint, and dedicated exact-head CI workflow.
2. Run the red integration gate and preserve its exact failure as construction evidence.
3. Complete the checked-in final witness manifest/case bank using existing owner case IDs/evidence identities only.
4. Run the focused integration gate to green.
5. Run all coupled owner/reference gates and the full repository/documentation matrix on the same exact head.
6. Review the complete base-to-head diff, inspect evidence/coverage/claim limits, remove any temporary transport, and reconcile `README.md`, `RESULTS.md`, `STATUS.md`, `next_step.yaml`, #36 and #122.
7. Freeze the exact reviewed/qualified head and stop for fresh repository-owner exact-head authorization before integrating it into `experimental/portfolio` if that gate remains applicable.

## Acceptance

This leaf is locally accepted only when one exact head produces one canonical final reference packet with all required owner evidence present and mutually consistent, all planned CUDA-free behavioral routes accounted for, optional deletion/multi-instance/schedule/multi-replica neutrality witnessed, all native-deferred work still deferred, zero product/native leakage, zero generated residue committed, and coupled qualification green.

Candidate integration remains distinct from protected #122 semantic acceptance.