{
  "schema_version": 3,
  "component": {
    "id": "integration.cuda-js-tensor-evaluator",
    "name": "CUDA-JS-Tensor Evaluator Connector",
    "status": "candidate",
    "product_area": "adapters",
    "path": "adapters/evaluators/cuda-js-tensor",
    "purpose": "Admit an injected public CUDA-JS-Tensor TensorDeviceProgram and emit a finite evaluator-owned restricted Device-JS runtime contribution for request incarnation, batching, Tensor item execution, stale-safe scatter and publication without taking over Tensor mathematics, Progress scheduling or lower CUDA ownership.",
    "roles": ["adapter", "evaluator-integration", "device-runtime-contribution"],
    "governing_authority": [
      "docs/specs/SPEC-0009-evaluator-contract.md",
      "docs/specs/SPEC-0011-finite-search-resources.md",
      "docs/specs/SPEC-0012-device-owned-search-progress.md",
      "docs/specs/SPEC-0015-native-boundary-and-js-only-implementation.md",
      "https://github.com/iteathen/CUDA-MCGS/issues/124"
    ],
    "design": {
      "owned_invariant": "An admitted TensorDeviceProgram retains one exact public callable/import identity, while MCGS-owned evaluator request/batch/result state uses finite generations and every post-batch work item revalidates its captured item/slot/request/batch incarnation before current-lane mutation or terminal publication.",
      "intended_equivalence_class": [
        "public CUDA-JS-Tensor TensorDeviceProgram capabilities with item-parallel callable semantics",
        "product-neutral scalar, vector or multi-output Tensor evaluator payloads",
        "finite request capacity with one first-realization active Tensor batch and partial-batch progress"
      ],
      "excluded_cases": [
        "Tensor mathematics or TensorProgram/TensorPlan interpretation",
        "domain/model/product input encoding semantics",
        "global Progress scheduler topology or worker selection",
        "model/checkpoint/head/product semantics",
        "native CUDA/provider mechanisms",
        "host-driven active evaluator progression"
      ],
      "authoritative_state_owner": "CUDA-JS-Tensor owns callable/item/workspace/library facts; CUDA-MCGS SPEC-0009 owns generated evaluator request/batch/result incarnation state; Progress owns service order; CUDA-JS owns lower Device-JS/native lifecycle.",
      "public_ports": ["adapters/evaluators/cuda-js-tensor/index.mjs"],
      "injected_dependencies": ["public cuda-js-tensor TensorDeviceProgram capability"],
      "adapter_boundaries": [
        "CUDA-JS-Tensor owns public Tensor callable/library identity and per-item Tensor math/workspace",
        "CUDA-MCGS evaluator owns request/batch/result generations, stale rejection, cancellation/retry and publication state",
        "selected evaluator profile/Search Program owns semantic encoded-input production",
        "Progress owns when/where ready evaluator work executes",
        "CUDA-JS owns Device-JS validation/lowering, public atomic/publication primitives, linking and native execution"
      ],
      "solid_responsibilities": [
        "admit public Tensor callable shape and exact import identity",
        "freeze finite request/batch/result/staging layouts",
        "emit token-validated Device-JS evaluator transitions",
        "preserve one-item partial-batch progress without host timing",
        "reject stale lane publication and import drift"
      ],
      "cupid_qualities": ["composable", "predictable", "domain-based", "fail-closed"],
      "second_instance": "A materially different conforming TensorDeviceProgram can use the same runtime contribution while changing Tensor shapes/math, and a non-Tensor evaluator remains free to replace this adapter entirely.",
      "first_consumer_deletion": "Removing Tensor evaluator selection removes the connector, imported Tensor callable and all Tensor-runtime-only code/state/resources; evaluator-free and non-Tensor profiles remain complete.",
      "essential_complexity": [
        "public callable/import identity",
        "finite request and batch generations",
        "captured stale-safe work token",
        "per-request encoded-input/result staging",
        "partial-batch formation",
        "single-publication authority and explicit terminal dispositions"
      ],
      "accidental_complexity_rejected": ["model semantics", "second Tensor compiler", "adapter-owned global scheduler", "private/deep imports", "native source", "host search progression loop", "shadow CUDA-JS helper catalog"],
      "complexity_moved_elsewhere": [
        "Tensor math/callable ABI/workspace remain CUDA-JS-Tensor",
        "semantic input encoding remains evaluator-profile/Search-Program owned",
        "service order/fairness/no-progress remain Progress owned",
        "allocation/operation/native linking/execution remain CUDA-JS"
      ],
      "simplest_sufficient_total_system": "Validate one public TensorDeviceProgram, generate finite evaluator-owned Device-JS transitions and layouts, carry exact lane-incarnation tokens through device work, and let the existing Search Program/Progress composition service those transitions through one CUDA-JS operation."
    },
    "public_contracts": [
      "cuda-mcgs.tensor-evaluator-connector/0.1.0",
      "cuda-mcgs.tensor-evaluator-device-runtime/0.1.0"
    ],
    "owns": [
      "Tensor evaluator connector admission and import drift rejection",
      "finite Tensor evaluator request/batch/result/staging runtime contribution",
      "slot/request/batch generation and stale-lane validation",
      "Tensor batch gather/scatter and terminal publication transitions"
    ],
    "persistent_state": [],
    "generated_artifacts": ["immutable connector descriptor", "immutable Device-JS runtime contribution descriptor/source", "portable conformance evidence"],
    "testing": {
      "test_owner": "conformance/cuda-js-tensor-evaluator",
      "authoritative_oracles": ["accepted SPEC-0009 evaluator semantics", "public CUDA-JS-Tensor TensorDeviceProgram contract", "public CUDA-JS restricted Device-JS/publication contracts"],
      "coverage_map_location": ["conformance/cuda-js-tensor-evaluator/README.md"],
      "focused_fast_capsules_and_commands": ["node scripts/run-cuda-js-tensor-evaluator.mjs", "node conformance/cuda-js-tensor-evaluator/package.mjs"],
      "owner_contract_capsules_and_commands": ["node scripts/run-evaluator-reference.mjs", "node scripts/run-progress-reference.mjs", "node scripts/run-search-ir-composer-reference.mjs"],
      "integration_smoke_dependencies": ["repository source-boundary policy", "CUDA-JS Device-JS inspection", "documentation/governance verification"],
      "deep_forensic_and_release_triggers": ["TensorDeviceProgram public ABI change", "evaluator request/batch/publication semantics change", "Device-JS atomic/publication contract change", "Progress runtime-entry integration"],
      "expected_discovery_and_skip_policy": ["portable connector/runtime capsule has zero native prerequisites and zero conditional skips"],
      "evidence_key_dimensions": ["connector/runtime source identity", "Tensor callable contract/item capacity/workspace/import identity", "request capacity and generated resource layout"],
      "test_invalidation_inputs": ["SPEC-0009", "SPEC-0011", "SPEC-0012", "TensorDeviceProgram public contract", "CUDA-JS Device-JS/publication contract", "connector/runtime source"],
      "shared_setup_and_mutable_state_isolation": ["each reference case uses fresh state; emitted-source oracle allocates fresh finite control/staging/result arrays"],
      "failure_cluster_and_repair_owner": "integration.cuda-js-tensor-evaluator for connector/runtime-transition defects; evaluator profile/Search Program for semantic encoding; Progress for service-order defects; CUDA-JS-Tensor for generic callable defects; CUDA-JS for lower Device-JS/native defects",
      "test_intent_consolidation_and_debt_policy": ["runtime falsifiers stay owner-local; conformance reference remains unexported; exact lower inspection and native qualification stay separate evidence layers"],
      "runtime_output_and_resource_budgets": ["bounded CUDA-free Node.js portable qualification; finite generated arrays only; no native/performance claim"]
    },
    "lifecycle_and_cleanup": {
      "startup_and_acquisition": ["validate public TensorDeviceProgram and finite capacities before creating connector/runtime descriptors", "all MCGS-owned control/staging/result bytes are explicit pre-ignition initialization requirements"],
      "steady_state_ownership": ["post-batch work carries captured item/slot/request/batch incarnation", "Progress services ready work; runtime transitions never poll the host", "shared Tensor inputs remain explicit pre-ignition external inputs"],
      "cancellation_and_failure": ["queued/inflight cancellation is ordered against one publication CAS", "Tensor failure maps to typed evaluator failure/retry state", "stale token mismatch is read-only and cannot mutate reused lane state", "generation exhaustion never wraps"],
      "teardown_and_resource_release": ["this slice emits finite owner resources and terminal states but does not yet own CUDA-JS allocations; final release/quarantine is composed by Resource/Progress/Search lifecycle integration"],
      "generated_cache_package_test_and_diagnostic_disposition": ["no persistent runtime cache", "portable emitted-source evidence is not native/provider evidence", "conformance reference remains physically excluded from package"],
      "persistence_migration_backup_and_recovery_retention": ["not applicable in this first runtime realization"],
      "archive_deprecation_and_supersession": ["public Tensor or CUDA-JS publication contract changes require explicit compatibility reassessment"],
      "cleanup_verification": ["import drift, pressure, cancellation, retry and stale-reused-lane portable falsifiers", "full Resource/Progress terminal cleanup remains required before #124 acceptance"]
    },
    "allowed_dependencies": ["injected public cuda-js-tensor capability values", "declared public CUDA-JS contract identifiers consumed by the existing CUDA-JS runtime adapter"],
    "forbidden_dependencies": ["cuda-js-tensor private/deep source", "CUDA-JS private/deep source", "native addons/FFI/CUDA/PTX", "product semantics", "Python", "adapter-owned global scheduler", "host gather/launch/poll/relaunch progression"],
    "known_consumers": ["conformance/cuda-js-tensor-evaluator", "Search Program/Progress composition for issue #124"],
    "validation": ["node scripts/run-cuda-js-tensor-evaluator.mjs", "node conformance/cuda-js-tensor-evaluator/package.mjs", "node scripts/run-evaluator-reference.mjs", "node scripts/run-search-ir-composer-reference.mjs"],
    "owner": "CUDA-MCGS framework"
  }
}
