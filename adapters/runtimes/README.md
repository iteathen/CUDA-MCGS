# Runtime adapters

Runtime adapters translate CUDA-MCGS execution requirements into versioned public runtime contracts.

- [CUDA-JS](cuda-js/README.md): implemented `integration.cuda-js` adapter; physical compatible-pair qualification remains separate.

Adapters own translation and assembled-resource lifecycle. Search semantics remain with the framework and native mechanisms remain with the peer runtime.
