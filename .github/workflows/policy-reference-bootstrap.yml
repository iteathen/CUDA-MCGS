name: policy-reference-bootstrap

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Policy bootstrap disabled while bounded payload chunks are staged.'
