# Legacy UMCGS Profile Compatibility Pointer

**Status:** Compatibility-only path. This file defines no independent project rules.

The project was renamed to **CUDA-MCGS**. The canonical current application profile is [`CUDA_MCGS_PROFILE.md`](CUDA_MCGS_PROFILE.md).

Any live instruction, plan, schema, implementation, or product boundary that still uses `UMCGS` as the current project identity must be interpreted as `CUDA-MCGS` unless it is explicitly preserved historical provenance. Do not copy this legacy filename into new paths or APIs.

Read and apply [`CUDA_MCGS_PROFILE.md`](CUDA_MCGS_PROFILE.md) now.
